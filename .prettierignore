# pnpm's lockfile has its own canonical format; formatting it with Prettier
# de-canonicalises it and makes every `pnpm install` rewrite ~24k lines.
pnpm-lock.yaml

# Generated code
packages/db/generated/
.next/
.expo/
.turbo/
node_modules/

# Reports/artifacts
lighthouse-*.json
lighthouse-*.html
